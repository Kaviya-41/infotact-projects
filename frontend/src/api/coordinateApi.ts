/**
 * coordinateApi.ts — Coordinate / Location API Service (Placeholder)
 *
 * Future purpose:
 * - Define functions to interact with coordinate/location-related backend endpoints.
 * - Examples of planned endpoints:
 *   - GET  /api/coordinates             — Fetch all vehicle coordinates
 *   - GET  /api/coordinates/:vehicleId  — Fetch coordinates for a specific vehicle
 *   - POST /api/coordinates             — Submit new coordinate data
 * - May also handle real-time coordinate updates via Socket.io in the future.
 * - All REST functions will use the shared Axios instance from ./axios.ts.
 * - Response types will be imported from ../types/.
 *
 * NOTE: Do not implement any API logic here yet.
 * This file is a placeholder for future backend integration.
 */
